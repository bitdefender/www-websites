class ButtonClickEvent {
    event;
    product = {};
    constructor(clickEvent, productId) {
        this.event = clickEvent;
        if (clickEvent === 'trial downloaded') {
            this.product = { trial: [{ ID: productId }] };
        }
        else if (clickEvent === 'product downloaded') {
            this.product = { download: [{ ID: productId }] };
        }
        else {
            this.product = { [clickEvent]: [{ ID: productId }] };
        }
    }
}

export { ButtonClickEvent };
//# sourceMappingURL=index.js.map
