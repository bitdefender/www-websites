interface:
  display_name: "EDS SharePoint to DA Import"
  short_description: "Audit and import one or many locale routes into DA"
  default_prompt: "Use $eds-sharepoint-da-import to audit one or more locale routes, including explicit indexless locales, produce separate indexed, non-indexed, and exclusion reports, then wait for import approval."
